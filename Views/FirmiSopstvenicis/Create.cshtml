@model Banka.Models.FirmiSopstvenici

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>FirmiSopstvenici</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="sopstvenikId" class="control-label">Сопственик</label>
                <select asp-for="sopstvenikId" class="form-control" asp-items="ViewBag.sopstvenikId">
                    <option value=" ">Select</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="firmaId" class="control-label">Фирма</label>
                <select asp-for="firmaId" class="form-control" asp-items="ViewBag.firmaId">
                    <option value=" ">Select</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
