@model AppUser
@{
    ViewData["Title"] = "Кориснички профил на корисник";
}

<h2>@(Model != null && Model.KorisnikId != null ? "Промени" : "Нов") кориснички профил</h2>
<h4>Корисник: @ViewData["FullName"]</h4>
<hr />
<div class="row">
    <form asp-action="KorisnikProfile" method="post">
        <div class="col-md-4">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group" hidden>
                <input name="KorisnikId" class="form-control" value="@(Model != null ? Model.KorisnikId : @ViewData["KorisnikId"])" hidden />
            </div>
            <div class="form-group">
                <label for="Id">ID на корисник</label>
                <input asp-for="Id" class="form-control" readonly />
            </div>
            <div class="form-group">
                <label for="PasswordHash">Хеширана лозинка</label>
                <input asp-for="PasswordHash" class="form-control" readonly />
            </div>
            <div class="form-group">
                <label for="Email">Email адреса</label>
                <input asp-for="Email" class="form-control" />
            </div>
            <div class="form-group">
                <label for="Password">Нова лозинка</label>
                <input name="Password" class="form-control" />
            </div>
            <hr />
            <div class="form-group">
                <input type="submit" value="@(Model != null && Model.KorisnikId != null ? "Зачувај" : "Внеси нов")" class="btn btn-default" />
            </div>
        </div>
    </form>
</div>
<div>
    <a asp-controller="Korisnici" asp-action="Index">Назад кон корисниците</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}